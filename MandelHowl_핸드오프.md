# MandelHowl 핸드오프

## 1. 문서 목적

이 문서는 학문 기반 「최악의 볼륨 조절 UI」 컨테스트 출품작 **MandelHowl**을 구현하기 위한 전체 설계 지도다. 구현자는 이 문서만으로 작품의 의도, 과학적 근거, 사용자 경험, 런타임 계산, 오프라인 물리 자산 생성, 안전 기준, 검증 기준을 일관되게 이해할 수 있어야 한다.

이 문서는 현재 구현 상황, 임시 우회, 완료 여부, 중간 체크포인트를 기록하지 않는다. 구현 과정에서 세부 기술은 교체할 수 있지만, 아래의 핵심 계약과 책임 경계는 유지해야 한다.

---

## 2. 프로젝트 한 줄 정의

> 사용자가 마우스로 주파수 다이얼 하나를 직접 돌려 **만델브로 부호화 질량·두께 분포를 가진 Chladni 진동판**을 가진하고, 가상 스피커–마이크 피드백 루프가 감쇠하면 볼륨 0, 포화되면 볼륨 100, 임계 경계에서만 1~99를 출력하는 과학 기반 볼륨 조절 UI.

작품명은 **MandelHowl**을 기본으로 사용한다. 이름은 `Mandelbrot`와 음향 피드백의 `Howl`을 결합한다.

---

## 3. 핵심 경험

사용자가 화면을 처음 보았을 때 설명 없이도 다음을 이해해야 한다.

1. 화면 중앙의 금속판과 모래는 소리에 반응하는 실험 장치다.
2. 금속판 아래의 커다란 스피커가 판을 진동시킨다.
3. 판을 향한 마이크와 되돌아가는 케이블은 음향 피드백 루프다.
4. `Hz`가 표시된 원형 다이얼은 가진 주파수를 조절한다.
5. `VOLUME` 또는 `dB` 미터는 결과로 나온 소리 크기다.
6. 주파수를 잘못 맞추면 진동이 죽고, 잘 맞추면 하울링이 폭주한다.

사용자에게 제공되는 **개념적 입력은 하나**, **개념적 출력도 하나**다.

- 입력: 마우스 또는 동등한 접근성 입력으로 직접 회전하는 `DRIVE FREQUENCY` 다이얼
- 출력: 0~100 정수로 표시되는 `VOLUME`

주파수 숫자, 오실로스코프, 모래 무늬, 잔향 표시 등은 입력과 출력의 원인을 설명하는 계측·시각 피드백이며 별도의 조절 입력으로 취급하지 않는다.

---

## 4. 작품이 의도적으로 만드는 불편함

일반적인 볼륨 슬라이더는 입력 위치와 출력 크기가 단조롭게 대응한다. MandelHowl은 이 관계를 고의로 파괴하되, 결과가 임의 난수처럼 보이지 않도록 실제 진동·피드백 개념으로 설명한다.

- 대부분의 주파수에서는 루프 이득이 임계값보다 낮아 진동이 사라지고 볼륨 0이 된다.
- 일부 공진 대역에서는 루프 이득과 위상이 자기진동 조건을 만족해 리미터까지 성장하고 볼륨 100이 된다.
- 1~99는 성장과 감쇠가 경쟁하는 좁은 임계 대역, 모드 간 비팅, 간헐적 피드백 버스트의 시간 평균에서만 나온다.
- 같은 최종 주파수라도 접근 방향, 회전 속도, 직전 잔향 때문에 결과가 달라질 수 있다.
- 이 차이는 숨겨진 랜덤이 아니라 화면에 보이는 판의 잔류 진동과 피드백 상태로 설명되어야 한다.

사용자는 무엇을 해야 하는지는 즉시 알지만, 원하는 중간 볼륨을 얻으려면 공진기의 상태를 읽고 손의 회전 속도와 접근 방향을 학습해야 한다.

---

## 5. 과학적 기반과 표현 한계

### 5.1 확립된 물리 현상

MandelHowl은 다음의 확립된 개념을 결합한다.

- 얇은 판의 고유진동 모드와 공진
- Chladni 무늬에서 입자가 마디선 부근에 모이는 현상
- 판의 형상, 두께, 질량, 강성, 고정 조건이 고유진동수와 모드 형상에 영향을 주는 현상
- 스피커–마이크 폐루프에서 루프 이득과 위상에 따라 피드백이 감쇠하거나 자기진동으로 성장하는 현상
- 리미터가 성장한 신호를 유한한 최대 진폭으로 포화시키는 현상
- 여러 고유모드가 동시에 활성화될 때 발생하는 비팅과 간섭

### 5.2 프로젝트의 창작적 결합

`만델브로–Chladni 이론`이라는 별도의 정립된 표준 이론이 있는 것은 아니다. 이 프로젝트는 만델브로 집합에서 얻은 유한 해상도 수치장을 실제 제작 가능한 판의 물성 분포로 변환하고, 그 판의 진동을 표준 판 진동 해석으로 계산하는 창작적 응용이다.

정확한 설명은 다음과 같다.

> 만델브로 반복식은 판의 형상 또는 물성 지도를 정의하는 데 사용되고, 실제 진동은 해당 물성을 가진 탄성판의 고유값 문제와 음향 피드백 모델이 결정한다.

다음 표현은 사용하지 않는다.

- “만델브로 반복식이 소리를 직접 생성한다.”
- “모래가 반드시 만델브로 모양으로 배열된다.”
- “무한 정밀도의 만델브로 구조를 실제로 제작했다.”
- “프랙탈만으로 출력이 자동으로 0과 100으로 양극화된다.”

### 5.3 물리적 판의 정식 정의

전면은 모래가 움직일 수 있도록 평평한 원형 또는 완만한 사각형 금속판으로 유지한다. 만델브로 정보는 뒷면의 두께, 부착 질량, 재료 밀도 또는 강성 분포 중 하나 이상에 부호화한다.

기본 설계는 다음을 권장한다.

- 평평한 원형 전면
- 중앙 고정 허브
- 자유 외곽 경계
- 뒷면의 유한 단계 만델브로 escape-time 기반 두께·질량 분포
- 명시된 최소 특징 크기와 최소 연결 두께
- 명시된 재료 밀도, 탄성계수, 포아송비, 감쇠비
- 명시된 가진점과 가상 마이크 측정점

이 설계는 첫눈에 Chladni 판으로 읽히면서도, 단면 또는 반투명 시각화에서 만델브로 부호화 구조를 드러낼 수 있다.

### 5.4 수치해석의 역할

브라우저 런타임은 편미분방정식이나 유한요소 행렬을 실시간으로 풀지 않는다. 대신 오프라인 물리 파이프라인에서 정의된 실제 판 모델을 해석하고 다음을 자산으로 굽는다.

- 고유진동수
- 정규화된 고유모드 변위장
- 마디선 마스크
- 표면 속도·가속도 분포
- 가진점 결합 계수
- 마이크 측정점 결합 계수
- 주파수별 복소 전달응답
- 모래 집결 예상 밀도
- 시각화용 변위·노멀·발광 텍스처
- 메시, 솔버, 재료, 경계조건, 수렴성 정보를 포함한 provenance

런타임 시각화는 임의로 그린 가짜 패턴이 아니라 이 계산 결과를 사용한다. 다만 입자 이동, 조명, 잔상, 발광은 과학적 데이터를 읽기 쉽게 표현하는 시각적 추상화로 취급한다.

---

## 6. 시스템 역할 분담

각 구성 요소가 맡는 물리적·UX적 역할을 섞지 않는다.

| 구성 요소 | 담당 역할 |
|---|---|
| 만델브로 수치장 | 판의 두께·질량·강성 분포를 정의해 복잡한 공진 스펙트럼을 만든다. |
| 탄성판 고유모드 | 특정 주파수에서 판이 어떤 공간 패턴으로 진동하는지 결정한다. |
| Chladni 모래 시각화 | 고유모드의 마디 구조를 눈에 보이게 만든다. |
| 가상 스피커 | 사용자가 선택한 주파수로 판을 가진한다. |
| 가상 마이크 | 판과 공기의 응답을 측정해 피드백 루프에 반환한다. |
| 피드백 엔진 | 감쇠, 성장, 버스트, 비팅, 포화 상태를 시간에 따라 계산한다. |
| 리미터 | 자기진동이 무한히 증가하지 않도록 가상 최대 진폭을 만든다. |
| 볼륨 매퍼 | 최근 측정 구간의 가상 음압 또는 RMS를 0~100 정수로 변환한다. |
| WebGL 렌더러 | 판, 모래, 스피커, 마이크, 케이블, 파형을 실시간으로 표현한다. |
| Web Audio 엔진 | 가상 결과를 안전한 실제 청취 레벨의 음향으로 변환한다. |
| 다이얼 엔진 | 포인터 궤적을 연속 회전각, 회전속도, 주파수로 변환한다. |

어떤 모듈도 다른 모듈의 책임을 임의로 복제하지 않는다. 예를 들어 렌더러가 독자적으로 볼륨을 계산하거나, 오디오 엔진이 별도의 공진 상태를 소유하면 안 된다.

---

## 7. 단일 입력 계약

### 7.1 다이얼 의미

다이얼은 `DRIVE FREQUENCY` 하나만 제어한다. 기본 매핑은 사람이 듣는 대역을 넓게 탐색할 수 있도록 로그 스케일을 사용한다.

- 최소 주파수와 최대 주파수는 런타임 설정에서 읽는다.
- 다이얼 각도는 연속적으로 풀어낸 unwrapped angle로 관리한다.
- 한 바퀴 경계를 넘을 때 값이 튀지 않아야 한다.
- 포인터가 중심 가까이 지나가더라도 각도 계산이 불안정해지지 않아야 한다.
- 다이얼은 화면상 여러 바퀴 회전할 수 있으나, 유효 주파수 범위를 넘으면 물리적인 끝단 저항을 표현한다.

### 7.2 마우스 조작

- 포인터를 누른 순간 다이얼 중심 기준 시작 각도를 기록한다.
- 이동마다 최단 각도 차이를 누적해 연속 회전량으로 변환한다.
- 중심에 너무 가까운 포인터 이동은 회전량 계산에서 제외한다.
- 포인터 캡처를 사용해 다이얼 밖으로 벗어나도 드래그를 유지한다.
- 놓는 순간 최근 각속도에서 제한된 관성을 계산한다.
- 회전 마찰은 일정하지 않고, 공진 에너지가 클수록 미세한 진동 저항을 시각·청각적으로 표현할 수 있다.

### 7.3 접근성 입력

마우스를 사용할 수 없는 경우에도 동일한 개념적 입력을 제공한다.

- 좌우 화살표: 작은 각도 회전
- PageUp/PageDown: 큰 각도 회전
- Home/End: 물리적 주파수 범위의 양 끝으로 이동
- 휠과 터치 회전: 동일한 다이얼 상태 갱신
- 스크린 리더에는 다이얼을 `role="slider"`로 노출하되, 시각적으로 선형 슬라이더를 만들지 않는다.

키보드와 터치는 별도의 입력 파라미터가 아니라 같은 회전 운동을 생성하는 어댑터다.

### 7.4 회전 이력

최종 주파수만으로 상태를 정하지 않는다. 다음 값은 물리적 가진 이력의 일부로 사용한다.

- 현재 주파수
- 직전 주파수
- 주파수 변화율
- 접근 방향
- 회전이 멈춘 시간
- 판에 남아 있는 모드별 진폭과 위상
- 피드백 리미터의 포락선

이력은 브루트포스를 방해하기 위한 숨은 규칙이 아니라, 연속적인 진동계의 상태다. 화면에서 잔류 진동과 파형으로 확인할 수 있어야 한다.

---

## 8. 단일 출력 계약

### 8.1 출력 정의

외부에 공개되는 핵심 출력은 정수 `0..100`이다.

- `0`: 가상 측정 신호가 노이즈 플로어 아래로 감쇠한 상태
- `100`: 자기진동이 성장해 가상 리미터 상한에 머무는 상태
- `1..99`: 임계 영역의 간헐적 버스트, 비팅, 불완전한 포화가 측정창에 부분적으로 포함된 상태

볼륨은 주파수의 직접 함수가 아니라 피드백 시스템의 최근 측정창에서 계산한다.

### 8.2 가상 볼륨과 실제 오디오의 분리

화면의 `VOLUME 100`은 운영체제나 기기의 실제 최대 음량을 의미하지 않는다. 실제 재생 신호는 별도의 안전 매핑을 거친다.

- 가상 볼륨: 작품 규칙을 위한 0~100 상태
- 실제 청취 게인: 사용자의 기기와 청력을 보호하기 위해 제한된 Web Audio 출력

실제 오디오는 하드 리미터, 최대 RMS 제한, 급격한 게인 점프 방지, 사용자 제스처 후 활성화 규칙을 반드시 따른다.

### 8.3 표시 규칙

- 숫자는 세 자리 고정폭으로 `000`~`100`을 표시한다.
- 바늘형 dB 미터 또는 세로 막대는 숫자와 같은 값을 읽는다.
- 계산 중에는 임의의 중간 숫자를 깜빡이지 않고 `MEASURING` 상태와 포락선을 보여준다.
- 값 확정 시 숫자, 바늘, 판의 상태가 같은 프레임에서 일치해야 한다.
- 챌린지 호스트가 목표값을 제공하는 경우 목표 표시는 읽기 전용 기준선이며 추가 입력이 아니다.

---

## 9. 런타임 공진·피드백 모델

### 9.1 데이터 기반 모달 모델

오프라인 자산에는 각 모드 `i`에 대해 다음 값이 포함된다.

- 고유 각주파수 `ωᵢ`
- 감쇠비 `ζᵢ`
- 가진 결합 계수 `gᵢ`
- 마이크 결합 계수 `mᵢ`
- 정규화 모드 텍스처 식별자
- 모드 부호와 위상 기준

런타임은 각 모드의 복소 진폭 또는 동등한 2차 상태를 시간 스텝으로 갱신한다. 시각화와 오디오는 같은 canonical modal state를 읽는다.

개념적 모달 응답은 다음과 같은 형태를 따른다.

```text
qᵢ'' + 2ζᵢωᵢqᵢ' + ωᵢ²qᵢ = gᵢ · drive(t) + feedbackᵢ(t)
```

정확한 이산화 방법은 구현에서 선택할 수 있으나 다음을 만족해야 한다.

- 프레임률과 시뮬레이션 결과가 직접 결합되지 않는다.
- 고정 시간 스텝 또는 안정적인 적분기를 사용한다.
- 브라우저 탭 정지 후 거대한 시간 점프를 한 번에 적분하지 않는다.
- 동일한 입력 이력과 데이터셋에서는 동일한 결과를 낸다.
- 핵심 계산에 `Math.random()`을 사용하지 않는다.

### 9.2 피드백 루프

가상 마이크 신호는 활성 모드의 측정점 응답을 합산해 구한다. 피드백은 지연, 필터, 이득, 위상을 거쳐 다시 가진 신호에 합쳐진다.

```text
스피커 가진
→ 판의 모달 응답
→ 가상 마이크 측정
→ 지연·필터·이득
→ 리미터 전 피드백
→ 스피커 가진으로 귀환
```

루프의 전역 파라미터는 버전 관리되는 설정 파일이 canonical owner다. 특정 목표 볼륨을 만들기 위한 숨은 주파수별 예외값을 코드에 하드코딩하지 않는다.

### 9.3 비선형 포화

극단값 분포는 다음의 실제적인 비선형 요소에서 나온다.

- 노이즈 게이트 또는 감쇠 임계
- 증폭기 소프트 클리핑
- 피크 리미터
- 포락선 팔로워
- 가상 스피커의 최대 변위 제한

피드백이 임계값을 넘으면 성장 속도가 빨라지고, 상한에서는 리미터가 진폭을 고정한다. 임계값 아래에서는 잔향이 감쇠한다. 이 구조가 0과 100을 안정된 두 극단으로 만든다.

### 9.4 히스테리시스와 잔향

- 성장 상태에서 주파수를 조금 벗어나도 잔류 에너지가 즉시 사라지지 않는다.
- 낮은 주파수에서 접근한 경우와 높은 주파수에서 접근한 경우의 위상 관계가 달라질 수 있다.
- 빠른 sweep은 모드가 성장할 시간을 주지 않으며, 느린 sweep은 특정 모드를 포획할 수 있다.
- 잔향은 모드별 에너지와 위상으로 저장하며 임의 타이머로 흉내 내지 않는다.

### 9.5 극단값 분포 목표

기본 데이터셋과 기본 피드백 설정은 다음 UX 목표를 만족하도록 보정한다.

- 충분히 감쇠한 초기 상태에서 정적 주파수 표본의 대부분이 0 또는 100으로 수렴한다.
- 1~99는 전체 주파수 범위에서 희귀하지만 모든 정수값이 적어도 하나 이상의 유효한 입력 이력으로 도달 가능하다.
- 중간값 대역은 연속 스캔만으로 쉽게 수집되지 않도록 폭이 좁고 상태 의존적이어야 한다.
- 각 중간값의 도달 가능성은 검증 도구가 재현 가능한 입력 궤적과 함께 증명한다.
- 도달성 확보를 위해 개별 정수값용 비밀 lookup table이나 결과 강제 분기를 넣지 않는다.

권장 기준은 감쇠 초기 상태의 균일 주파수 샘플에서 0 또는 100의 비중이 90% 이상이 되도록 시작하되, 실제 작품 재미와 조작 가능성을 보며 전역 피드백 파라미터로 조정한다.

---

## 10. 오프라인 물리 자산 파이프라인

### 10.1 canonical 입력 사양

모든 물리 자산은 사람이 읽을 수 있는 버전 관리 사양에서 생성한다.

- 판 외곽 치수
- 전면 형상
- 중심 고정 허브 크기
- 뒷면 두께 범위
- 재료 상수
- 만델브로 좌표 범위와 반복 상한
- escape-time 정규화 방식
- 최소 특징 크기
- 제조 가능성 필터
- 메시 밀도 규칙
- 가진점과 마이크점
- 추출할 모드 수와 주파수 범위
- 감쇠 모델
- 텍스처 해상도와 포맷

생성된 메시나 텍스처를 직접 수정하지 않는다. 수정은 항상 원본 사양 또는 생성기 코드에서 시작한다.

### 10.2 만델브로 물성장 생성

만델브로 반복은 물성 분포를 만들기 위한 입력장이다.

1. 판 좌표를 복소평면 좌표로 변환한다.
2. 각 위치에서 반복 탈출 횟수와 필요 시 distance estimate를 계산한다.
3. 값을 연속 또는 계단형 0~1 수치장으로 정규화한다.
4. 최소 특징 크기보다 작은 구조를 제조 가능성 필터로 정리한다.
5. 정규화장을 두께, 부착 질량 또는 밀도 변화로 매핑한다.
6. 전체 질량, 무게중심, 최소 강성, 최소 두께 제한을 검증한다.

무한 프랙탈을 주장하지 않는다. 데이터셋에는 반복 상한, 샘플 해상도, 필터 크기, 실제 최소 특징 크기를 명시한다.

### 10.3 메시와 유한요소 해석

기본 솔버 어댑터는 얇은 판 또는 shell 요소의 고유값 해석을 지원해야 한다.

- 입력 사양에서 물리 형상과 물성장을 생성한다.
- 메시 품질과 최소 요소 크기를 검증한다.
- 중앙 고정과 자유 외곽 경계를 명시한다.
- 고유값과 고유벡터를 추출한다.
- 모드 정규화와 부호 기준을 통일한다.
- 최소 두 단계 이상의 메시 해상도에서 주요 고유주파수 수렴을 확인한다.
- 선택된 저차·중차 모드는 독립적인 참조 계산 또는 축소 모델과 교차검증한다.

솔버는 교체 가능하게 어댑터로 분리하지만, 데이터셋 manifest에는 사용한 솔버 이름, 버전, 옵션, 컨테이너 이미지 digest를 기록한다.

### 10.4 모드 후처리

각 모드에서 다음을 계산한다.

- 표면 변위와 속도 크기
- 마디선 또는 저속 영역 마스크
- 가진점 결합 계수
- 가상 마이크 결합 계수
- 모드별 방사 효율의 단순화 계수
- 인접 모드와의 주파수 간격
- 렌더링 좌표계와 물리 좌표계 변환

모드 부호는 임의이므로 텍스처 프레임 간 부호가 뒤집혀 깜빡이지 않도록 기준점을 정해 정규화한다.

### 10.5 Chladni 모래 텍스처

모래 밀도는 임의 장식이 아니라 시간 평균 표면 속도가 낮은 영역으로 이동하는 현상을 시각적으로 근사한 후처리 결과다.

- 마디선 부근의 밀도를 높인다.
- 경계 바깥과 고정 허브에는 모래를 생성하지 않는다.
- 실제 입자 크기보다 작은 선은 적절히 병합한다.
- 너무 날카로운 1픽셀 선은 렌더링 해상도에 맞게 보정한다.
- 모래 분포와 변위 텍스처의 좌표가 정확히 일치해야 한다.

런타임은 모드별 모래 밀도 텍스처를 모달 에너지에 따라 혼합하고, 전환 속도와 잔류 입자 효과만 동적으로 처리한다.

### 10.6 런타임 데이터셋 패키징

배포 데이터셋은 content-addressed 디렉터리에 저장한다.

- `manifest.json`
- `plate-spec.json`
- `modes.bin` 또는 동등한 압축 바이너리
- `response.bin`
- `textures/*.ktx2`
- `provenance.json`
- `convergence-report.json`
- `coverage-report.json`
- `checksums.json`

웹 앱은 파일명을 추측하지 않고 manifest를 통해 모든 자산을 찾는다. 모든 파일은 해시 검증 후 사용한다.

---

## 11. 시각 디자인

### 11.1 첫 화면의 시각적 우선순위

1. 커다란 스피커 콘
2. 그 위 또는 앞의 금속 Chladni 판과 모래
3. 판을 향한 마이크
4. 마이크→증폭기→스피커로 되돌아가는 굵은 케이블
5. 손으로 돌리는 `Hz` 다이얼
6. 큰 `VOLUME` 숫자와 계기판
7. 보조 오실로스코프
8. 판 뒷면의 만델브로 물성 분포를 암시하는 반투명 단면

프랙탈 이미지를 전체 배경의 주인공으로 만들지 않는다. 화면을 처음 본 사람이 “프랙탈 탐색기”가 아니라 “기묘한 음향 실험 장치”로 인식해야 한다.

### 11.2 판 표현

- 전면은 실제 금속판처럼 평평하게 보인다.
- 뒤쪽 또는 가장자리 단면에 두께 변화가 드러난다.
- 선택적으로 짧은 X-ray pulse에서만 만델브로 물성 분포가 보인다.
- 모래 무늬는 만델브로 실루엣을 그대로 따라가지 않고 계산된 마디선에 따라 형성된다.
- 공진이 강할수록 판 변위, 조명 반사, 모래 집결이 선명해진다.

### 11.3 스피커와 마이크

- 스피커 콘은 주파수와 가상 진폭에 따라 실제로 왕복한다.
- 마이크 주변에는 측정되는 음압을 나타내는 작고 절제된 파동 링을 둔다.
- 피드백이 성장할 때 케이블을 따라 신호 펄스가 순환한다.
- 케이블 애니메이션은 장식이 아니라 신호 흐름을 설명해야 한다.

### 11.4 오실로스코프

오실로스코프는 다음 중 현재 상태를 가장 잘 설명하는 한 가지 신호를 표시한다.

- 마이크 파형
- 피드백 포락선
- 입력과 반환 신호의 위상 차

여러 그래프를 동시에 늘어놓지 않는다. 기본은 마이크 파형이며, 임계 영역에서 위상 차를 짧게 강조할 수 있다.

### 11.5 DDD 피드백

도파민 포인트는 별도의 점수·코인·배지보다 물리 현상에서 나온다.

- 공진 접근: 모래가 흩어진 상태에서 선을 만들기 시작한다.
- 모드 포획: 모래 무늬가 순간적으로 또렷하게 잠긴다.
- 성장: 케이블 펄스와 스피커 변위가 빠르게 커진다.
- 포화: 미터 바늘이 100에 부딪히고 리미터가 작동한다.
- 근접 중간값: 무늬가 완성될 듯하다가 비팅으로 무너진다.
- 잔향: 사용자가 곧바로 다시 돌리고 싶게 만드는 시각적 흔적을 남긴다.

근접 실패를 과도한 도박식 문구로 자극하지 않는다. 물리계의 불안정성과 붕괴가 자체적으로 실패를 보여주게 한다.

### 11.6 모션 안전

- 강한 번쩍임을 반복하지 않는다.
- 감소된 모션 환경에서는 판 변위와 카메라 흔들림을 줄인다.
- 핵심 정보는 색상만으로 전달하지 않는다.
- 공진 포화 시 화면 전체 플래시 대신 국소 발광과 계기판 반응을 사용한다.

---

## 12. 오디오 디자인과 안전

### 12.1 오디오의 역할

오디오는 과학적 원인과 결과를 체감하게 하는 보조 출력이다. 실제 기기 볼륨을 조절하지 않는다.

- 드라이브 톤은 현재 주파수를 반영한다.
- 판의 모달 응답은 선택된 모드의 가중 합으로 음색을 변화시킨다.
- 피드백 성장과 감쇠는 포락선으로 들린다.
- 100에서도 청취 레벨은 안전한 상한에 제한된다.

### 12.2 활성화 규칙

- 브라우저 자동재생 제한을 우회하지 않는다.
- 첫 포인터 또는 키보드 제스처 후 AudioContext를 시작한다.
- 시작 전에는 화면 시뮬레이션이 동작하되 실제 음향은 재생하지 않는다.
- 오디오가 비활성인 상태를 명확히 표시하되 별도의 필수 설정 입력으로 만들지 않는다.

### 12.3 안전 체인

실제 출력 직전에 다음 순서를 적용한다.

```text
가상 모달 오디오
→ DC 제거
→ 대역 제한
→ 소프트 클리퍼
→ RMS 제한기
→ 피크 리미터
→ 급격한 게인 변화 완화
→ AudioDestination
```

- `VOLUME 100`을 실제 0 dBFS 지속 신호로 매핑하지 않는다.
- 고주파 순음을 장시간 최대 레벨로 재생하지 않는다.
- 포화 시 음색은 하울링의 느낌을 유지하되 실제 귀에 거슬리는 에너지를 제한한다.
- 탭 비활성화, 페이지 숨김, 오류 발생 시 오디오 게인을 즉시 안전하게 낮춘다.

### 12.4 마이크 권한

실제 마이크 권한을 요청하지 않는다. 화면의 마이크는 가상 측정점이며, 피드백은 내부 모델로 계산한다. 실제 마이크 입력을 사용하면 환경·하드웨어·개인정보에 따라 결과가 달라지고 안전한 재현성을 잃으므로 핵심 범위에서 제외한다.

---

## 13. 아키텍처 원칙

### 13.1 canonical ownership

- 판과 재료의 정의: `specs/plate/*`
- 피드백 전역 파라미터: `specs/runtime/*`
- 직렬화 계약: `packages/contracts`
- 다이얼 상태: `packages/dial-engine`
- 모달·피드백 상태: `packages/resonance-engine`
- 실제 청취 게인: `packages/audio-engine`
- GPU 자산과 렌더 패스: `packages/render-engine`
- 생성된 데이터셋: 오프라인 파이프라인의 파생 산출물

같은 값의 사본을 여러 파일에서 수동 관리하지 않는다. 파생 값은 생성기로 만들어야 한다.

### 13.2 의존성 방향

```text
contracts
   ↑
   ├── dial-engine
   ├── resonance-engine
   ├── asset-runtime
   ├── diagnostics
   ├── audio-engine
   └── render-engine
             ↑
          apps/web
```

- 핵심 엔진은 DOM과 WebGL 객체를 직접 참조하지 않는다.
- 렌더러와 오디오는 resonance snapshot을 읽고 명령을 수행한다.
- 웹 앱은 엔진을 조율하지만 물리 공식을 재구현하지 않는다.
- 오프라인 Python 도구와 TypeScript 런타임은 JSON Schema 및 manifest 계약으로 연결한다.

### 13.3 결정성

- 핵심 출력은 데이터셋, 초기 상태, 시간 스텝, 다이얼 입력 이력으로 완전히 결정되어야 한다.
- 시각적 노이즈가 필요하면 데이터셋 해시와 세션 고정 시드에서 생성한다.
- 테스트 재현성을 위해 시계와 애니메이션 프레임을 주입 가능하게 만든다.
- 코어 엔진은 실제 벽시계나 DOM 이벤트를 직접 읽지 않는다.

### 13.4 실패 처리

진단은 추측 대신 확인 가능한 코드와 증거를 제공한다.

- `confirmed`: 원인이 검증됨
- `needs_evidence`: 필요한 정보가 아직 로드되지 않음
- `inconclusive`: 여러 원인을 구분할 수 없음

데이터셋 해시 불일치, WebGL2 미지원, KTX2 디코딩 실패, AudioContext 거부, 물리 데이터 누락 등을 서로 다른 진단 코드로 분리한다. 사용자 화면에는 이해 가능한 메시지를, 개발 모드에는 구조화 보고서를 제공한다.

---

## 14. 데이터 계약

### 14.1 plate spec

판 사양은 다음 필드를 가진 버전된 계약으로 관리한다.

- `schemaVersion`
- `plateId`
- `geometry`
- `boundaryCondition`
- `material`
- `mandelbrotField`
- `thicknessMapping`
- `massMapping`
- `actuator`
- `virtualMicrophone`
- `frequencyRange`
- `solverRequest`
- `textureRequest`

### 14.2 resonance dataset manifest

- 데이터셋 식별자와 버전
- 판 사양 해시
- 솔버 provenance
- 모드 수와 주파수 범위
- 바이너리 파일 위치와 해시
- 텍스처 atlas 위치와 해시
- 좌표계와 단위
- 런타임 호환 버전
- 수렴 보고서 위치
- 도달성 보고서 위치

### 14.3 runtime snapshot

렌더러와 오디오 엔진에 전달되는 snapshot은 다음을 포함한다.

- 시뮬레이션 시간
- 다이얼 각도와 주파수
- 모드별 진폭·위상 또는 압축 표현
- 마이크 RMS와 피크
- 피드백 포락선
- 상태 분류: `decaying`, `critical`, `growing`, `saturated`
- 확정 볼륨
- 측정 진행도
- 진단 상태

렌더러는 내부 적분 상태를 직접 수정하지 않는다.

---

## 15. 성능 설계

### 15.1 런타임 원칙

- PDE, 메시 해석, 고유값 계산은 런타임에서 수행하지 않는다.
- 모달 상태는 필요한 활성 모드만 갱신한다.
- 프레임마다 객체와 배열을 새로 만들지 않는다.
- 시뮬레이션 고정 스텝과 렌더링 프레임을 분리한다.
- 텍스처는 KTX2 등 GPU 친화 압축을 사용하고 필요 모드를 지연 로드한다.
- 자산 로딩 중에는 낮은 해상도 안전 fallback을 사용한다.

### 15.2 목표 예산

초기 구현의 성능 목표는 다음과 같이 둔다.

- 데스크톱 기본 렌더: 60 FPS 목표
- 저성능·절전 환경: 30 FPS 유지 가능한 단계적 품질 저하
- 메인 스레드 장시간 작업 방지
- 초기 UI shell은 물리 자산과 분리해 빠르게 표시
- 대형 모드 atlas는 manifest 단위로 지연 로드
- 오디오 callback에서 동적 할당과 로그 출력 금지

정확한 용량 수치는 실제 자산 품질 측정 후 예산 파일에서 관리하며 문서와 코드에 중복 하드코딩하지 않는다.

### 15.3 품질 저하 순서

성능이 부족할 때 물리 출력의 의미를 바꾸지 않고 표현만 줄인다.

1. 모래 입자 잔상 감소
2. 판 표면 노멀 해상도 감소
3. 간접광·후처리 비활성화
4. 오실로스코프 샘플 밀도 감소
5. 렌더링 내부 해상도 감소
6. Canvas2D 기반 데이터 일치 fallback

모달 상태, 피드백 계산, 최종 볼륨은 품질 단계와 관계없이 동일해야 한다.

---

## 16. 검증 전략

### 16.1 물리 데이터 검증

- 판 질량과 두께가 물리 제한을 벗어나지 않는지 확인
- 메시 뒤집힘, 음수 면적, 최소 품질 위반 확인
- 고유주파수가 오름차순이고 양수인지 확인
- 모드 정규화와 부호 기준 확인
- 질량 행렬 기준 모드 직교성 확인
- 메시 세분화에 따른 주요 주파수 수렴 확인
- 가진점과 마이크 결합 계수가 유한한지 확인
- 텍스처와 모드 좌표계가 일치하는지 확인

### 16.2 런타임 엔진 검증

- 동일 입력 trace에서 동일 출력이 나오는지 확인
- 프레임률 변화가 결과에 영향을 주지 않는지 확인
- 긴 탭 정지 후 안전하게 재개되는지 확인
- 0 상태가 유한 시간 안에 노이즈 플로어로 감쇠하는지 확인
- 100 상태가 리미터 상한을 넘지 않는지 확인
- 임계 영역에서 NaN, 무한대, 폭주가 없는지 확인
- 모든 1~99가 검증된 입력 trace로 도달 가능한지 확인

### 16.3 UI·입력 검증

- 0/360도 경계에서 다이얼이 튀지 않는지 확인
- 포인터 캡처가 손실되어도 세션을 정리하는지 확인
- 빠른 회전, 느린 회전, 역회전의 속도 계산 확인
- 키보드 입력이 같은 다이얼 도메인 명령을 만드는지 확인
- 숫자, 계기판, 오디오, 판 애니메이션이 같은 snapshot을 읽는지 확인
- 목표값이 있을 때 목표는 읽기 전용인지 확인

### 16.4 오디오 안전 검증

- 모든 가상 상태에서 실제 피크·RMS 상한 준수
- AudioContext 오류 시 출력 게인 차단
- 페이지 숨김과 종료 시 fade-out
- limiter bypass 경로 부재
- 잘못된 dataset 값이 오디오 노드 파라미터에 직접 전달되지 않음

### 16.5 시각 회귀 검증

대표 상태를 고정 snapshot으로 렌더링해 비교한다.

- 완전 감쇠
- 약한 비공진
- 임계 접근
- 간헐적 버스트
- 강한 성장
- 리미터 포화
- 잔향 중 역회전
- 감소된 모션 모드
- Canvas fallback

---

## 17. 보안·개인정보·운영 원칙

- 실제 마이크, 카메라, 위치 권한을 요청하지 않는다.
- 사용자 입력 trace를 외부로 전송하지 않는다.
- 분석 도구는 기본 비활성이고 핵심 기능에 필요하지 않다.
- 데이터셋과 셰이더는 manifest allowlist와 해시 검증을 거친다.
- 외부 HTML을 주입하지 않는다.
- Content Security Policy를 적용한다.
- 생성 자산 provenance와 제3자 라이선스를 배포물에 포함한다.
- 오류 보고서에는 개인 정보나 전체 브라우저 환경을 무분별하게 포함하지 않는다.

---

## 18. 구현 순서

아래 순서는 의존성 기준의 권장 흐름이며 완료 상태를 나타내지 않는다.

### A0. 프로젝트 계약 고정

- 프로젝트 명칭, 단위계, 좌표계, 데이터 버전을 정의한다.
- plate spec, resonance manifest, runtime snapshot의 JSON Schema를 작성한다.
- 생성물과 원본 사양의 canonical ownership을 명시한다.
- 핵심 엔진의 결정성 규칙과 진단 상태를 정의한다.

### A1. 다이얼 도메인 엔진

- unwrapped angle 계산
- 포인터 샘플의 이상치 제거
- 각속도와 방향 계산
- 로그 주파수 매핑
- 제한된 관성과 끝단 저항
- 키보드·터치 어댑터
- 재현 가능한 gesture trace 포맷

### A2. 데이터 없는 공진 코어

- 합성 모드 fixture를 사용하는 고정 스텝 적분기
- 가상 마이크 합산
- 피드백 지연과 필터
- 게이트, 소프트 클리핑, 리미터
- RMS 측정창과 0~100 매핑
- 감쇠·임계·성장·포화 상태 분류

### B0. 판 사양과 만델브로 물성장

- 물리 치수와 재료 정의
- 좌표 변환과 escape-time 계산
- 최소 특징 크기 필터
- 두께·질량 매핑
- 제조 가능성 및 전체 질량 검증
- 시각화를 위한 단면 메타데이터 생성

### B1. 메시·솔버 어댑터

- 판 형상 메시 생성
- 경계조건과 물성 배치
- 고유값 해석 실행
- 결과 파싱과 정규화
- 솔버 오류를 구조화 진단으로 변환
- 독립 참조 해석을 위한 어댑터 경계 유지

### B2. 수렴성과 provenance

- 메시 수준별 주파수 비교
- 주요 모드의 공간 유사도 비교
- 솔버·컨테이너·옵션 기록
- 판 사양과 생성물 해시 연결
- 재현 가능한 generation report 생성

### C0. 모드 후처리

- 가진·마이크 결합 계수 계산
- 주파수 응답 생성
- 마디선과 모래 밀도 계산
- signed displacement와 normal map 생성
- 모드 부호 정규화

### C1. 런타임 자산 패키징

- 모달 바이너리 직렬화
- KTX2 텍스처 atlas 생성
- content-addressed 디렉터리 구성
- manifest와 checksums 생성
- schema·해시·좌표 검증

### D0. WebGL 장면

- 카메라와 레이아웃
- 스피커, 판, 마이크, 케이블 메시
- 판 변위 셰이더
- 모래 밀도 합성
- 신호 흐름 애니메이션
- 오실로스코프와 계기판 presenter

### D1. 웹 애플리케이션 조율

- 자산 로딩과 capability probe
- 다이얼 명령을 시뮬레이션에 전달
- 고정 스텝 tick loop
- snapshot을 렌더러·오디오·DOM에 배포
- 페이지 수명주기와 오류 복구

### D2. 안전한 오디오

- 사용자 제스처 기반 AudioContext 시작
- 모달 음색 합성
- 피드백 포락선 반영
- 안전 체인과 상한 검증
- 탭 비활성·오류 시 fade-out

### E0. 극단값 보정과 도달성

- 정적 주파수 분포 측정
- 상태 의존 trajectory 탐색
- 0~100 도달성 보고서 생성
- 전역 이득·감쇠·리미터 파라미터 조정
- 비밀 per-value 예외가 없는지 검사

### E1. DDD 연출

- 모래가 모드에 잠기는 순간
- 성장·감쇠·포화 전환
- 임계 비팅과 근접 실패 표현
- 잔향과 케이블 순환 신호
- 감소된 모션에서도 의미가 유지되는 표현

### F0. 진단·fallback·접근성

- 데이터셋 무결성 진단
- WebGL2·KTX2·오디오 capability 분기
- Canvas fallback
- 키보드·스크린 리더 동작
- 구조화 개발 보고서와 사용자 메시지 분리

### F1. 통합 검증

- gesture trace 기반 결정성 테스트
- physics asset 검증
- 오디오 안전 테스트
- 시각 회귀
- 장시간 메모리·성능 테스트
- 목표값이 제공되는 챌린지 harness 테스트

### G0. 배포 패키징

- 웹 shell과 자산 manifest 연결
- 캐시 무효화와 dataset 버전 고정
- 제3자 라이선스와 provenance 포함
- 정적 호스팅용 보안 헤더
- 재현 가능한 release artifact 생성

---

## 19. 완료 품질 기준

작품은 다음 조건을 동시에 만족해야 한다.

1. 첫 화면만 보고도 스피커, 마이크, 금속판, Hz 다이얼, 볼륨 출력의 인과관계를 이해할 수 있다.
2. 사용 가능한 개념적 조절 입력은 회전 다이얼 하나뿐이다.
3. 핵심 볼륨 출력은 0~100 정수 하나다.
4. 대부분의 안정 입력은 0 또는 100으로 수렴한다.
5. 모든 1~99가 재현 가능한 물리 상태 이력으로 도달 가능하다.
6. 핵심 출력에 임의 난수나 정수별 숨은 lookup table을 사용하지 않는다.
7. 화면의 Chladni 무늬는 오프라인 해석 결과와 연결된 텍스처에서 나온다.
8. 만델브로 정보는 장식이 아니라 판의 물성 사양에 실제로 반영된다.
9. 모래 무늬가 만델브로 실루엣과 같다고 잘못 주장하지 않는다.
10. 가상 볼륨 100이 실제 기기 최대 음량으로 매핑되지 않는다.
11. 실제 마이크 권한 없이 동일한 결과를 재현한다.
12. 동일 데이터셋과 입력 trace에서 결과가 결정적이다.
13. WebGL 품질 저하나 fallback이 핵심 볼륨 계산을 바꾸지 않는다.
14. 데이터셋은 사양, 솔버, 메시, 수렴성, 해시 provenance를 포함한다.
15. 오류는 추측이 아니라 확인 가능한 진단 코드와 근거로 보고한다.
16. 키보드·터치·스크린 리더가 동일한 다이얼 의미를 사용할 수 있다.
17. 감소된 모션에서도 공진 상태와 출력 변화를 이해할 수 있다.
18. 장시간 실행해도 메모리와 오디오 노드가 누적되지 않는다.

---

## 20. 명시적 비범위

다음 기능은 프로젝트 정체성과 직접 관련이 없으므로 핵심 범위에 포함하지 않는다.

- 실제 운영체제 마스터 볼륨 제어
- 실제 마이크 입력을 사용한 물리 공간 피드백
- 사용자가 스피커·마이크·판 위치를 추가로 조절하는 다중 입력 UI
- 만델브로 좌표 직접 탐색기
- 실시간 범용 유한요소 해석기
- 사용자가 재료·메시·솔버를 편집하는 연구용 CAD UI
- 계정, 서버 저장, 소셜 기능, 분석용 개인정보 수집
- 점수, 가상 화폐, 광고, 확률형 보상
- 과학적으로 검증되지 않은 효과를 실제 현상처럼 단정하는 설명

---

## 21. 최종 전달 문장

MandelHowl의 성공 여부는 복잡한 그래픽 양이 아니라 **인과관계의 선명함**으로 판단한다. 관객은 스피커가 판을 울리고, 모래가 공진을 보여주고, 마이크가 소리를 되돌려 피드백을 만들며, 그 결과가 볼륨이 된다는 사실을 즉시 읽어야 한다. 만델브로는 이 흐름을 가리는 장식이 아니라, 판의 물성에 실제로 들어가 공진 스펙트럼을 복잡하게 만드는 수학적 원인이어야 한다.
