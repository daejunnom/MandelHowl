{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mandelhowl.dev/schemas/resonance-trajectory-trace.v1.schema.json",
  "title": "MandelHowl deterministic resonance trajectory v1",
  "$ref": "#/$defs/trace",
  "$defs": {
    "contentId": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "keyframe": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sequence", "atSeconds", "frequencyHz"],
      "properties": {
        "sequence": { "type": "integer", "minimum": 0 },
        "atSeconds": { "type": "number", "minimum": 0 },
        "frequencyHz": {
          "type": "number",
          "minimum": 45,
          "maximum": 6000
        }
      }
    },
    "trace": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schemaVersion",
        "traceId",
        "modalModelId",
        "initialFrequencyHz",
        "durationSeconds",
        "expectedSettledVolume",
        "keyframes"
      ],
      "properties": {
        "schemaVersion": {
          "const": "mandelhowl.resonance-trajectory-trace.v1"
        },
        "traceId": { "type": "string", "minLength": 1 },
        "modalModelId": { "$ref": "#/$defs/contentId" },
        "initialFrequencyHz": {
          "type": "number",
          "minimum": 45,
          "maximum": 6000
        },
        "durationSeconds": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "expectedSettledVolume": {
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "keyframes": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/keyframe" }
        }
      }
    }
  }
}
